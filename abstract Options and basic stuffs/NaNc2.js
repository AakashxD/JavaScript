/** type of NaN 
 * it doesnt actually means not a number, rather it gives notion of an invalid number
 *  NaN is the only primitive value in js thhhat doesnt folow identity property and is not equal.
 * 
*/
console.log(NaN===NaN);
let x;
console.log(typeof(x));

